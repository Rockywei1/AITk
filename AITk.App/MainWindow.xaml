<Window x:Class="AITk.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AITk - AI Toolkit" 
        Height="720" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" 
                      GlassFrameThickness="0" 
                      CornerRadius="12"
                      ResizeBorderThickness="6"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="12" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>   <!-- Title Bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════════════════════════ -->
            <!-- Custom Title Bar                                        -->
            <!-- ═══════════════════════════════════════════════════════ -->
            <Border Grid.Row="0" 
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="12,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Logo & Title -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                Margin="16,0">
                        <TextBlock Text="⚡" 
                                   FontSize="20" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <TextBlock Text="AITk" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text=" Desktop" 
                                   FontSize="16" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" 
                                Orientation="Horizontal"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Content="─" 
                                Click="MinimizeClick"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="16,8" Margin="0"/>
                        <Button Content="□" 
                                Click="MaximizeClick"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="16,8" Margin="0"/>
                        <Button Content="✕" 
                                Click="CloseClick"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="16,8" Margin="0,0,8,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════ -->
            <!-- Main Content Area                                       -->
            <!-- ═══════════════════════════════════════════════════════ -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>  <!-- Sidebar -->
                    <ColumnDefinition Width="*"/>    <!-- Content -->
                </Grid.ColumnDefinitions>

                <!-- Sidebar Navigation -->
                <Border Grid.Column="0" 
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Nav Items -->
                        <StackPanel Grid.Row="0" Margin="0,16,0,0">
                            <RadioButton x:Name="NavDashboard"
                                         Content="📊  Dashboard"
                                         Style="{StaticResource NavButtonStyle}"
                                         IsChecked="True"
                                         Checked="NavChanged"/>
                            <RadioButton x:Name="NavRunner"
                                         Content="🏃  Task Runner"
                                         Style="{StaticResource NavButtonStyle}"
                                         Checked="NavChanged"/>
                            <RadioButton x:Name="NavReview"
                                         Content="🔍  Smart Review"
                                         Style="{StaticResource NavButtonStyle}"
                                         Checked="NavChanged"/>
                            <RadioButton x:Name="NavPolisher"
                                         Content="✨  Code Polisher"
                                         Style="{StaticResource NavButtonStyle}"
                                         Checked="NavChanged"/>
                            <RadioButton x:Name="NavPRReview"
                                         Content="📋  PR Review"
                                         Style="{StaticResource NavButtonStyle}"
                                         Checked="NavChanged"/>
                        </StackPanel>

                        <!-- Version Info -->
                        <TextBlock Grid.Row="1" 
                                   Text="v1.0.0 · Mode 2 (Tool)"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="16,0,0,16"/>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <Border Grid.Column="1" 
                        Background="{StaticResource BackgroundBrush}"
                        CornerRadius="0,0,12,0">
                    <ContentControl x:Name="ContentArea" 
                                    Margin="24"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
